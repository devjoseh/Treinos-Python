#Conta quantos caracteres existe numa frase. Exemplo:
# A frase usada foi: bom dia a todos. Resultado:
# A frase digitada possui 15 caracteres.

frase = input("Digite uma frase: ")
num_caracteres = len(frase)
print("A frase digitada possui", num_caracteres, "caracteres.")